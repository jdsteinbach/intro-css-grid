import Reveal from 'reveal.js'
import { initHighlightingOnLoad } from 'highlight.js'

Reveal.initialize({
  controls: true,
  progress: true,
  history: true,
  center: true,
  width: 1000,
  height: 800,
  transition: 'convex'
});


initHighlightingOnLoad()
