import Reveal from 'reveal.js'
import hljs from 'highlight.js'

Reveal.initialize({
  controls: true,
  progress: true,
  history: true,
  center: true,
  width: 960,
  height: 800,
  transition: 'convex'
});

hljs.initHighlightingOnLoad()
